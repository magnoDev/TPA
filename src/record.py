class Record:

    def __init__(self, algorithm, times, min, max, avg):
        self.algorithm = algorithm
        self.times = times
        self.min = min
        self.max = max
        self.avg = avg
